# 产品定义

> **当前基线说明（2026-08-02）**：课程内容已调整为 109 个连续教学章节、8 个独立模块大作业章节、8 个模块；第 10 章文件操作为普通教学章节，并采用模块末大作业。本文早期版本中的 75 章、7 模块和章节练习数量已按当前基线更新；平台服务接口仍以“未来可接入能力”单独描述。

## 1. 产品名称

暂定名称：Python Data Studio  
中文描述：Python 数据分析课程工作台

## 2. 产品目标

让初学者在一个浏览器页面中完成以下闭环：

1. 找到课程章节；
2. 阅读概念和示例；
3. 运行或修改代码；
4. 查看表格、图表和错误；
5. 保存学习过程；
6. 下载 Notebook 作为个人成果。

产品重点不是复制 Colab 或 JupyterLab 的全部功能，而是提供更清晰的课程结构、更稳定的 Notebook 运行环境和更适合教学的视觉层级。

## 3. 目标用户

### 主要用户

- 第一次系统学习 Python 的学生；
- 希望通过 Notebook 学习数据分析的非计算机专业用户；
- 需要 NumPy、Pandas、Matplotlib、Seaborn 和 Plotly 入门课程的学习者。

### 次要用户

- 需要演示课程的教师；
- 需要检查 Notebook 内容和执行状态的课程维护者。

## 4. 用户能力假设

- 会使用浏览器、文件和基本键盘操作；
- 不要求安装本地 Python；
- 不要求理解虚拟环境、包管理和数据库；
- 不假设用户了解 JupyterLab 的工作区概念。

## 5. 核心用户任务

| 任务 | 预期结果 |
|---|---|
| 选择章节 | 在当前页面打开对应 Notebook |
| 阅读课程 | Markdown 具有稳定的阅读排版 |
| 运行代码 | 明确显示排队、运行、成功或错误 |
| 修改代码 | 自动记录未保存状态 |
| 查看数据 | 可以查看 DataFrame、CSV 和图表输出 |
| 保存学习过程 | 工作副本保存在浏览器中 |
| 下载成果 | 导出标准 `.ipynb` 文件 |
| 恢复课程 | 可以恢复当前 Notebook 的原始版本 |
| 继续学习 | 返回时恢复最近章节和学习进度 |

## 6. 产品范围

### 首期包含

- 109 个连续教学章节 Notebook；
- 8 个模块大作业 Notebook；
- 1 个课程环境初始化 Notebook（不计入章节进度）；
- 课程树、搜索和最近访问；
- JupyterLite + Pyodide 浏览器运行时；
- Notebook 编辑、执行、保存、下载和恢复；
- Python 运行状态和错误反馈；
- MUI X 数据查看器；
- 学习进度概览；
- 浅色主题；
- 桌面、平板和移动端布局；
- 无障碍和减少动画支持。

### 首期不包含

- 用户账号；
- 云同步；
- 数据库；
- 多人实时协作；
- GPU；
- 远程 Jupyter Server；
- 教师后台；
- 章节作业自动评分；
- 模块大作业自动评分（当前采用教师/学习者自检与结果解释，不作为首期必需能力）；
- 自定义插件市场；
- 完整 IDE 文件管理能力。

## 7. 内容范围

课程按八个模块组织：

1. Python 基础：第 1–11 章；
2. NumPy：第 12–16 章；
3. Pandas：第 17–25 章；
4. Matplotlib：第 26–36 章；
5. Seaborn：第 37–55 章；
6. Plotly：第 56–72 章；
7. 综合项目：第 73–76 章；
8. 机器学习：第 77–109 章。

`00_课程环境初始化.ipynb` 作为运行环境准备内容，不计入正式章节进度。

## 8. 设计原则

### 内容优先

课程和 Notebook 内容始终拥有最高视觉优先级。导航和控制区应安静、紧凑、可预测。

### 运行状态明确

用户必须能判断 Python 是否正在加载、已就绪、正在运行或发生错误。

### 渐进披露

初学者首先看到阅读、运行和保存；高级设置、数据检查与重置放在二级界面中。

### 稳定胜过装饰

任何动效都不能改变单元格尺寸、遮挡代码或造成布局跳动。

### 原始内容可恢复

课程源文件保持不可变，用户编辑发生在浏览器工作副本中，并始终提供恢复入口。

## 9. 成功指标

首期以可验证的产品指标为准：

- 新用户能在 30 秒内打开第一章；
- 首次运行代码时能明确看到运行时加载进度；
- 所有 109 个连续教学章节 Notebook 和 8 个独立模块大作业 Notebook 均可打开；
- 代码章节可以运行并生成输出；
- 刷新页面后用户修改仍存在；
- 用户能下载标准 Notebook；
- 任意支持视口下不存在顶栏、菜单和单元格重叠；
- 第一章以计算机基础、Python 语言和 Jupyter Notebook 为主，可包含轻量可运行示例；
- 第一章打开时不主动加载 Pyodide，只有用户首次运行代码时再加载运行时；
- 关键操作全部可通过键盘完成。
