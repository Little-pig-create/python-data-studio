# Python 数据工作台 · 学生使用手册

> 本手册适用于学生角色用户，介绍平台的核心功能与常见操作。

---

## 目录

1. [登录与身份](#1-登录与身份)
2. [界面总览](#2-界面总览)
3. [课程学习](#3-课程学习)
4. [运行代码](#4-运行代码)
5. [学习进度](#5-学习进度)
6. [搜索与导航](#6-搜索与导航)
7. [学习记录与备份](#7-学习记录与备份)
8. [常见问题](#8-常见问题)

---

## 1. 登录与身份

打开平台后会进入登录页，使用学号和密码登录。登录后系统会根据你的角色自动跳转到对应主页（学生 → 课程工作区）。

- 忘记密码请联系任课老师或学校管理员，平台本身不存储密码明文。
- 一个账号对应一个角色，学生账号无法访问教师工作台。

---

## 2. 界面总览

```
┌──────────────┬─────────────────────────────────────────────┐
│              │  顶栏：章节标题 · 运行状态 · 操作按钮        │
│   左侧目录   ├─────────────────────────────────────────────┤
│   （侧边栏）  │                                             │
│              │         Notebook 工作区                     │
│  模块列表    │   代码单元格 · Markdown 说明 · 输出结果      │
│  章节列表    │                                             │
│              ├─────────────────────────────────────────────┤
│  学习记录    │  底栏：上一章 / 下一章 导航                  │
│  我的实训    │                                             │
└──────────────┴─────────────────────────────────────────────┘
```

**侧边栏折叠**：点击侧边栏顶部的 `‹` / `›` 图标可折叠/展开。折叠后显示各模块进度圆点，悬停可查看章节完成数。

---

## 3. 课程学习

### 课程结构

平台包含 8 个模块，共 108 章：

| 模块 | 章节范围 | 内容 |
|------|----------|------|
| Python 基础 | 第 1–10 章 | 语法、数据类型、函数 |
| NumPy | 第 11–15 章 | 数组计算 |
| Pandas | 第 16–24 章 | 数据处理 |
| Matplotlib | 第 25–35 章 | 静态可视化 |
| Seaborn | 第 36–54 章 | 统计可视化 |
| Plotly | 第 55–71 章 | 交互可视化 |
| 综合项目 | 第 72–75 章 | 真实业务项目 |
| 机器学习 | 第 76–108 章 | 模型训练与评估 |

### 打开章节

在左侧目录点击任意章节名称即可打开对应 Notebook。  
首次加载某章节时，系统会从本地读取课程文件，无需网络。

### 编辑与草稿

- 你对 Notebook 的所有修改（运行结果、添加代码、注释）都会自动保存为**本地草稿**。
- 草稿保存在浏览器 IndexedDB 中，不影响原始课程文件。
- 如果想恢复原始内容，点击顶栏的 **"重置"** 按钮。

---

## 4. 运行代码

### 运行单个单元格

点击代码单元格左侧的 **▶ 运行** 按钮，或使用快捷键 `Shift + Enter`。

### 运行全部

点击顶栏 **"全部运行"** 按钮，从第一个代码单元格依次执行到最后一个。  
全部运行成功且无报错时，章节会自动标记为完成（进度变为 100%）。

### 运行状态指示

顶栏右侧会显示内核状态：
- 🟢 **就绪** — 可以运行代码
- 🟡 **运行中** — 正在执行，请等待
- 🔴 **错误** — 内核异常，可尝试重启内核

### 输出说明

- 文字输出直接显示在单元格下方
- 图表（Matplotlib / Seaborn / Plotly）以图片或交互图形内嵌显示
- 错误信息以红色显示，包含行号和错误类型，可根据提示修改代码

---

## 5. 学习进度

### 进度计算方式

每个章节的进度 = 已运行代码单元格数 ÷ 章节总代码单元格数。

- 章节内所有代码单元格至少运行一次后，进度达到 100%
- 手动点击 **"标记完成"** 按钮也可强制标记为 100%
- 进度以圆形进度环显示在章节名称旁边

### 查看整体进度

侧边栏展开状态下，每个模块标题后显示 `已完成/总计` 章节数（例如 `Python 基础 (3/10)`）。

点击左侧导航的 **"学习记录"** 可进入总览页面，查看：
- 全课程完成百分比
- 各模块柱状图
- 最近学习的章节列表（带进度和笔记标记）

---

## 6. 搜索与导航

### 搜索

侧边栏展开后，顶部有搜索框，支持按以下内容检索：
- 章节标题（如"线性回归"）
- 模块名（如"Pandas"）
- 标签（如"项目"、"基础"）

搜索结果实时显示，点击即可跳转。

### 章节导航

Notebook 底栏提供 **"← 上一章"** 和 **"下一章 →"** 按钮，可顺序翻阅章节，无需返回目录。

---

## 7. 学习记录与备份

### 笔记

你可以在每个章节的笔记区域添加文字笔记，笔记保存在本地，在"学习记录"页面可看到有笔记的章节标注。

### 备份与恢复

进入 **"学习记录"** 页面，找到 **"备份与恢复"** 区域：

- **导出备份**：将当前所有进度数据（完成状态、笔记）打包为 JSON 文件下载到本地
- **导入备份**：上传之前导出的 JSON 文件，恢复进度

> ⚠️ 备份文件只保存进度和笔记，不包含 Notebook 草稿内容。清除浏览器数据前请先导出备份。

---

## 8. 常见问题

**Q：代码运行后没有任何输出？**  
A：检查代码中是否有 `print()` 语句，或者变量是否放在最后一行（Notebook 会自动显示最后一行表达式的值）。

**Q：图表没有显示？**  
A：Matplotlib 需要在代码末尾加 `plt.show()`，或确保 `%matplotlib inline` 已执行。Plotly 图表会直接内嵌，通常无需额外操作。

**Q：运行时显示"内核错误"或一直转圈？**  
A：刷新页面重新加载章节，首次打开某章节时内核初始化可能需要 5–15 秒。

**Q：我修改了代码，怎么恢复原始内容？**  
A：点击顶栏的 **"重置"** 按钮，确认后会清除草稿，恢复原始课程内容。

**Q：换了电脑，学习进度还在吗？**  
A：进度保存在浏览器本地（localStorage / IndexedDB），换电脑后不会自动同步。请在旧设备使用"导出备份"，再在新设备"导入备份"。

**Q：能下载 Notebook 文件吗？**  
A：可以。在 Notebook 顶栏点击 **"下载"** 图标，会将当前草稿保存为 `.ipynb` 文件。

---

*Python 数据工作台 · 如有其他问题请联系任课教师*
