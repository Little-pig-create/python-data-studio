const example = (title, explanation, code) => ({ title, explanation, code });

// 每个代码单元格只演示一个核心操作，并尽量自带最小输入。
export const foundationIndependentExamples = {
  2: [
    example("type() 查看类型", "先确认数据类型，再决定可以执行哪些运算。", "values = ['Python', 2026, 3.14, True, None]\nfor value in values:\n    print(repr(value), '->', type(value).__name__)"),
    example("数值运算", "把中间结果保存为有意义的变量，便于检查公式。", "price = 128.0\nquantity = 3\ndiscount = 30.0\nsubtotal = price * quantity\npayable = subtotal - discount\nprint('小计:', subtotal)\nprint('应付:', payable)"),
    example("类型转换", "读取文本数据后，常常需要显式转换为数值。", "raw_quantity = '3'\nraw_price = '128.50'\nquantity = int(raw_quantity)\nprice = float(raw_price)\nprint(quantity, type(quantity).__name__)\nprint(price, type(price).__name__)"),
    example("比较运算", "比较表达式的结果是布尔值，可以直接用于业务规则。", "amount = 880\nprint('是否达到500元:', amount >= 500)\nprint('是否等于880元:', amount == 880)\nprint('是否不是负数:', amount != -1)"),
    example("and、or、not", "先把每个条件命名，再组合复杂规则。", "amount = 880\nis_member = True\nis_blocked = False\neligible = amount >= 500 and is_member and not is_blocked\nneeds_review = amount >= 5000 or is_blocked\nprint('可以享受优惠:', eligible)\nprint('需要人工复核:', needs_review)"),
  ],
  4: [
    example("append()", "append把一个元素放到列表末尾。", "scores = [85, 92, 78]\nscores.append(96)\nprint(scores)"),
    example("extend()", "extend会逐个加入可迭代对象中的元素。", "scores = [85, 92]\nscores.extend([78, 96])\nprint(scores)"),
    example("insert()", "insert(index, value)在指定位置插入一个元素。", "cities = ['上海', '广州', '北京']\ncities.insert(1, '深圳')\nprint(cities)"),
    example("remove()", "remove删除第一次出现的匹配值。", "items = ['键盘', '鼠标', '键盘']\nitems.remove('键盘')\nprint(items)"),
    example("pop()", "pop适合既要删除又要使用被删除元素的场景。", "orders = ['A101', 'A102', 'A103']\nlast_order = orders.pop()\nprint('已处理:', last_order)\nprint('剩余:', orders)"),
    example("index()", "index定位第一次出现的位置，找不到时会抛出ValueError。", "values = [10, 20, 10, 30, 10]\nprint('第一个10的位置:', values.index(10))"),
    example("count()", "count统计一个值在列表中出现的次数。", "values = [10, 20, 10, 30, 10]\nprint('10出现次数:', values.count(10))"),
    example("sort()", "sort在原列表上排序，返回值是None。", "scores = [85, 92, 78]\nresult = scores.sort()\nprint('排序后的列表:', scores)\nprint('返回值:', result)"),
    example("sorted()", "sorted返回排序后的新列表，不修改原列表。", "scores = [85, 92, 78]\nordered = sorted(scores, reverse=True)\nprint('原列表:', scores)\nprint('降序副本:', ordered)"),
    example("reverse()", "reverse在原列表上反转元素顺序。", "steps = ['输入', '清洗', '分析', '输出']\nsteps.reverse()\nprint(steps)"),
    example("copy()", "copy复制列表的外层结构，适合避免简单的引用共享。", "steps = ['输入', '清洗', '分析']\nbackup = steps.copy()\nbackup.append('输出')\nprint('原列表:', steps)\nprint('副本:', backup)"),
    example("clear()", "clear删除列表中的所有元素，但保留列表对象。", "items = ['键盘', '鼠标']\nitems.clear()\nprint(items)"),
  ],
  "tuple": [
    example("元组创建与索引", "元组适合保存有固定顺序的记录。", "record = ('A102', '华东', 1280.5)\nprint(record[0])\nprint(record[-1])"),
    example("单元素元组", "单元素元组必须保留逗号。", "one_item = ('Python',)\nnot_tuple = ('Python')\nprint(type(one_item).__name__)\nprint(type(not_tuple).__name__)"),
    example("count()", "count统计元素在元组中出现的次数。", "values = ('待处理', '完成', '完成')\nprint(values.count('完成'))"),
    example("index()", "index查找元素第一次出现的位置。", "values = ('待处理', '完成', '完成')\nprint(values.index('完成'))"),
    example("位置解包", "按照顺序把元组中的值分配给变量。", "record = ('A102', '华东', 1280.5)\norder_id, region, amount = record\nprint(order_id, region, amount)"),
    example("星号解包", "星号变量可以收集中间的多个元素。", "steps = ('输入', '清洗', '分析', '输出')\nfirst, *middle, last = steps\nprint(first)\nprint(middle)\nprint(last)"),
    example("元组拼接与重复", "使用加号拼接元组，使用乘号重复元组。", "left = ('A', 'B')\nright = ('C',)\nprint(left + right)\nprint(('提示',) * 2)"),
    example("不可变性", "元组不能直接修改元素；需要修改时先转换为列表。", "record = ('A102', '华东')\neditable = list(record)\neditable[1] = '华南'\nrecord = tuple(editable)\nprint(record)"),
  ],
  5: [
    example("get()", "键不存在时，get可以返回默认值而不是抛出KeyError。", "order = {'id': 'A102', 'amount': 1280.5}\nprint(order.get('amount', 0))\nprint(order.get('coupon', 0))"),
    example("keys()、values()、items()", "三个视图分别用于遍历键、值和键值对。", "order = {'id': 'A102', 'amount': 1280.5}\nprint(list(order.keys()))\nprint(list(order.values()))\nprint(list(order.items()))"),
    example("update()", "update可以一次增加或覆盖多个键。", "profile = {'name': '小林', 'level': 1}\nprofile.update({'level': 2, 'city': '上海'})\nprint(profile)"),
    example("setdefault()", "键不存在时设置默认值，已存在时保留原值。", "groups = {}\ngroups.setdefault('华东', []).append('A102')\ngroups.setdefault('华东', []).append('A103')\nprint(groups)"),
    example("集合去重", "set会自动删除重复元素，适合统计不同成员。", "regions = ['华东', '华南', '华东', '华北']\nunique_regions = set(regions)\nprint(unique_regions)\nprint('地区数量:', len(unique_regions))"),
    example("集合交集、并集与差集", "集合运算适合比较两批用户或商品的共同与独有成员。", "january = {'u1', 'u2', 'u3'}\nfebruary = {'u2', 'u3', 'u4'}\nprint('共同成员:', january & february)\nprint('全部成员:', january | february)\nprint('一月独有:', january - february)"),
    example("add() 与 discard()", "add加入元素，discard删除元素且元素不存在时不报错。", "tags = {'python', 'pandas'}\ntags.add('numpy')\ntags.discard('old')\nprint(tags)"),
  ],
  6: [
    example("if 与 else", "条件成立时运行if分支，否则运行else分支。", "amount = 880\nif amount >= 500:\n    label = '重点订单'\nelse:\n    label = '普通订单'\nprint(label)"),
    example("if、elif、else", "分支从上到下判断，先写更严格或更具体的条件。", "score = 87\nif score >= 90:\n    level = '优秀'\nelif score >= 80:\n    level = '良好'\nelif score >= 60:\n    level = '合格'\nelse:\n    level = '需改进'\nprint(level)"),
    example("in 判断成员关系", "in适合判断一个值是否存在于列表、集合或字典中。", "allowed_regions = {'华东', '华南'}\nregion = '华东'\nprint(region in allowed_regions)"),
    example("条件表达式", "只在二选一且表达式很短时使用条件表达式。", "inventory = 3\nstatus = '库存紧张' if inventory < 5 else '库存充足'\nprint(status)"),
    example("组合业务条件", "先拆出布尔变量，再组合复杂规则，便于检查每一部分。", "amount = 1280\nis_member = True\nregion = '华东'\namount_ok = amount >= 1000\nregion_ok = region in {'华东', '华南'}\neligible = amount_ok and (is_member or region_ok)\nprint(eligible)"),
  ],
  7: [
    example("range()", "range适合遍历连续整数，右端点不包含。", "for number in range(1, 6):\n    print(number)"),
    example("enumerate()", "enumerate同时提供位置和值，避免手动维护索引。", "cities = ['上海', '广州', '北京']\nfor index, city in enumerate(cities, start=1):\n    print(index, city)"),
    example("zip()", "zip可以同时遍历多个等长序列。", "regions = ['华东', '华南', '华北']\nsales = [1280, 960, 1100]\nfor region, amount in zip(regions, sales):\n    print(region, amount)"),
    example("列表推导式", "简单的映射或筛选可以用列表推导式表达。", "scores = [58, 72, 88, 95]\npassed = [score for score in scores if score >= 60]\nscaled = [round(score / 100, 2) for score in scores]\nprint('及格分数:', passed)\nprint('比例:', scaled)"),
    example("while 与 break", "while适合由条件决定循环次数，必须确保最终会结束。", "balance = 1000\nmonths = 0\nwhile balance < 1200:\n    balance *= 1.05\n    months += 1\n    if months > 24:\n        break\nprint(months, round(balance, 2))"),
    example("continue", "continue跳过当前轮，但循环状态仍必须继续推进。", "values = [120, -1, 180, 0, 210]\ntotal = 0\nfor value in values:\n    if value <= 0:\n        continue\n    total += value\nprint(total)"),
  ],
  8: [
    example("def 定义函数", "函数把一项明确任务封装起来，可以重复调用。", "def greet(name):\n    return f'你好，{name}'\n\nprint(greet('小林'))"),
    example("return 返回结果", "return把结果交给调用方，print只负责显示。", "def calculate_total(price, quantity):\n    return price * quantity\n\namount = calculate_total(128, 3)\nprint(amount)"),
    example("默认参数", "默认参数适合稳定、简单的默认规则。", "def shipping_fee(amount, threshold=99):\n    return 0 if amount >= threshold else 8\n\nprint(shipping_fee(128))\nprint(shipping_fee(80, threshold=120))"),
    example("关键字参数", "使用参数名调用可以提高可读性。", "def summarize(name, total, unit='元'):\n    return f'{name}: {total:.2f} {unit}'\n\nprint(summarize(name='销售额', total=1280.5))"),
    example("多返回值", "多个返回值会组合成元组，可以直接解包。", "def describe(values):\n    return min(values), max(values), sum(values) / len(values)\n\nminimum, maximum, average = describe([12, 18, 25])\nprint(minimum, maximum, average)"),
    example("lambda 与 sorted 的 key", "lambda适合短小的一次性函数，复杂逻辑应使用def。", "orders = [{'id': 'A1', 'amount': 320}, {'id': 'A2', 'amount': 880}, {'id': 'A3', 'amount': 460}]\nranked = sorted(orders, key=lambda order: order['amount'], reverse=True)\nprint(ranked)"),
  ],
  9: [
    example("Path 组合路径", "Path对象比手写字符串拼接更适合跨平台处理路径。", "from pathlib import Path\n\nroot = Path.cwd()\ndata_path = root / 'data' / 'orders.csv'\nprint(data_path)\nprint(data_path.suffix)"),
    example("write_text() 与 read_text()", "文本读写时显式指定UTF-8编码。", "from pathlib import Path\nfrom tempfile import TemporaryDirectory\n\nwith TemporaryDirectory() as folder:\n    path = Path(folder) / 'notes.txt'\n    path.write_text('第一行\\\\n第二行', encoding='utf-8')\n    print(path.read_text(encoding='utf-8'))"),
    example("exists()", "读取文件前先检查路径是否存在，可以给出更清晰的提示。", "from pathlib import Path\n\npath = Path('notebook_demo.txt')\nprint('是否存在:', path.exists())"),
    example("glob()", "glob可以按模式查找指定目录下的文件。", "from pathlib import Path\nfrom tempfile import TemporaryDirectory\n\nwith TemporaryDirectory() as folder:\n    root = Path(folder)\n    for name in ['a.csv', 'b.csv', 'notes.txt']:\n        (root / name).touch()\n    print(sorted(path.name for path in root.glob('*.csv')))"),
    example("with open() 自动关闭文件", "with代码块结束后文件会自动关闭，资源管理更安全。", "from tempfile import TemporaryDirectory\n\nwith TemporaryDirectory() as folder:\n    path = f'{folder}/log.txt'\n    with open(path, 'w', encoding='utf-8') as file:\n        file.write('任务完成\\\\n')\n    with open(path, 'r', encoding='utf-8') as file:\n        print(file.readline().strip())"),
  ],
  10: [
    example("捕获 ValueError", "把预期的格式错误转换为用户能理解的提示。", "raw = 'not-a-number'\ntry:\n    value = float(raw)\nexcept ValueError:\n    value = None\n    print('输入不是有效数字')\nprint(value)"),
    example("捕获多个异常类型", "多个异常有相同处理方式时可以放在同一个元组中。", "values = [10, 0]\ntry:\n    result = values[0] / values[1]\nexcept (ZeroDivisionError, IndexError) as error:\n    print('计算失败:', type(error).__name__)"),
    example("else", "else只在try代码没有异常时运行，适合放成功后的逻辑。", "raw = '128.5'\ntry:\n    amount = float(raw)\nexcept ValueError:\n    print('金额格式错误')\nelse:\n    print('金额读取成功:', amount)"),
    example("finally", "finally无论是否出错都会执行，适合释放资源或记录状态。", "try:\n    print('开始处理')\n    result = 10 / 2\nexcept ZeroDivisionError:\n    print('除数不能为0')\nfinally:\n    print('处理结束')"),
    example("raise 主动校验", "当输入违反业务规则时，用raise主动抛出明确异常。", "amount = -10\ntry:\n    if amount < 0:\n        raise ValueError('金额不能为负数')\nexcept ValueError as error:\n    print('校验失败:', error)"),
  ],
  11: [
    example("np.array()", "array把Python序列转换为NumPy数组，并提供shape和dtype等属性。", "import numpy as np\n\nvalues = np.array([10, 20, 30])\nprint(values)\nprint('shape:', values.shape)\nprint('dtype:', values.dtype)"),
    example("zeros() 和 ones()", "用固定值创建指定形状的数组，适合初始化矩阵。", "import numpy as np\n\nprint(np.zeros((2, 3)))\nprint(np.ones((2, 3)))"),
    example("arange()", "arange生成等步长序列，右端点通常不包含。", "import numpy as np\n\nprint(np.arange(0, 10, 2))"),
    example("linspace()", "linspace按点数控制结果，适合绘图和连续模拟。", "import numpy as np\n\nprint(np.linspace(0, 1, 5))"),
    example("reshape()", "reshape要求元素总数不变，只改变数组的组织方式。", "import numpy as np\n\nvalues = np.arange(12)\nmatrix = values.reshape(3, 4)\nprint(matrix)\nprint(matrix.shape)"),
    example("astype()", "astype返回转换后的新数组，原数组不会被原地修改。", "import numpy as np\n\nvalues = np.array([1.2, 2.8, 3.4])\nintegers = values.astype(int)\nprint(integers)\nprint(integers.dtype)"),
  ],
  12: [
    example("二维索引", "二维数组使用行索引和列索引定位单个元素。", "import numpy as np\n\nmatrix = np.array([[10, 20, 30], [40, 50, 60]])\nprint(matrix[1, 2])"),
    example("切片", "切片左闭右开，可以同时选择行和列。", "import numpy as np\n\nmatrix = np.arange(12).reshape(3, 4)\nprint(matrix[:2, 1:3])"),
    example("布尔筛选", "布尔数组可以筛选满足条件的元素。", "import numpy as np\n\nvalues = np.array([12, 5, 18, 3, 20])\nprint(values[values >= 10])"),
    example("np.where()", "where可以根据条件从两个值或数组中选择结果。", "import numpy as np\n\nvalues = np.array([52, 78, 91])\nlabels = np.where(values >= 60, '及格', '不及格')\nprint(labels)"),
    example("高级索引", "使用整数索引数组可以按指定顺序提取多个位置。", "import numpy as np\n\nvalues = np.array([10, 20, 30, 40, 50])\nprint(values[[4, 1, 3]])"),
  ],
  13: [
    example("reshape() 与 transpose()", "reshape改变形状，transpose交换维度，不改变元素总数。", "import numpy as np\n\nmatrix = np.arange(6).reshape(2, 3)\nprint(matrix.reshape(3, 2))\nprint(matrix.transpose())"),
    example("concatenate()", "concatenate沿已有轴拼接，其他轴的形状必须兼容。", "import numpy as np\n\nleft = np.array([[1, 2], [3, 4]])\nright = np.array([[5, 6], [7, 8]])\nprint(np.concatenate([left, right], axis=0))"),
    example("stack()", "stack会创建一个新的维度，适合把同形状数组组成批次。", "import numpy as np\n\nfirst = np.array([1, 2, 3])\nsecond = np.array([4, 5, 6])\nprint(np.stack([first, second], axis=0))"),
    example("split()", "split按指定位置把一个数组拆成多个数组。", "import numpy as np\n\nvalues = np.arange(8)\nparts = np.split(values, 4)\nprint([part.tolist() for part in parts])"),
    example("hsplit() 与 vsplit()", "hsplit按列拆分，vsplit按行拆分。", "import numpy as np\n\nmatrix = np.arange(12).reshape(3, 4)\nprint([part.tolist() for part in np.hsplit(matrix, 2)])\nprint([part.tolist() for part in np.vsplit(matrix, 3)])"),
  ],
  14: [
    example("向量化加减", "数组运算会逐元素执行，不需要手写for循环。", "import numpy as np\n\nsales = np.array([100, 120, 150])\nadjusted = sales * 1.1\nprint(adjusted)"),
    example("np.sqrt()", "NumPy通用函数可以批量处理整个数组。", "import numpy as np\n\nvalues = np.array([1, 4, 9, 16])\nprint(np.sqrt(values))"),
    example("广播", "一维数组可以沿兼容的维度与二维数组运算。", "import numpy as np\n\nsales = np.array([[100, 120, 140], [80, 90, 110]])\nfactor = np.array([1.0, 1.05, 1.1])\nprint(sales * factor)"),
    example("np.where() 向量化条件", "where可以一次性给整列数值打标签。", "import numpy as np\n\namount = np.array([120, 580, 1280])\nlabels = np.where(amount >= 500, '重点', '普通')\nprint(labels)"),
    example("np.clip()", "clip把过小或过大的值限制在指定区间内。", "import numpy as np\n\nscores = np.array([-5, 65, 108])\nprint(np.clip(scores, 0, 100))"),
  ],
  15: [
    example("sum()、mean() 和 median()", "均值和中位数要结合分布与异常值一起解释。", "import numpy as np\n\nvalues = np.array([10, 12, 13, 15, 100])\nprint('总和:', values.sum())\nprint('均值:', values.mean())\nprint('中位数:', np.median(values))"),
    example("axis 聚合", "axis=0按列聚合，axis=1按行聚合，使用前先确认业务维度。", "import numpy as np\n\nsales = np.array([[100, 120, 140], [80, 90, 110]])\nprint('每月:', sales.sum(axis=0))\nprint('每地区:', sales.sum(axis=1))"),
    example("quantile()", "分位数可以描述分布位置，并帮助构造异常阈值。", "import numpy as np\n\nvalues = np.array([10, 12, 13, 15, 18, 20, 100])\nq1, q3 = np.quantile(values, [0.25, 0.75])\nprint(q1, q3)"),
    example("default_rng()", "固定种子可以让练习结果可复现；旧版NumPy使用RandomState作为兼容替代。", "import numpy as np\n\nif hasattr(np.random, 'default_rng'):\n    rng = np.random.default_rng(42)\n    values = rng.integers(1, 11, size=5)\nelse:\n    rng = np.random.RandomState(42)\n    values = rng.randint(1, 11, size=5)\nprint(values)"),
    example("choice()", "choice可以抽取样本，replace=False表示不重复。", "import numpy as np\n\nif hasattr(np.random, 'default_rng'):\n    rng = np.random.default_rng(7)\nelse:\n    rng = np.random.RandomState(7)\npopulation = np.arange(1, 21)\nprint(rng.choice(population, size=5, replace=False))"),
    example("normal() 和 poisson()", "不同分布适合模拟不同类型的随机现象，参数要写清楚。", "import numpy as np\n\nif hasattr(np.random, 'default_rng'):\n    rng = np.random.default_rng(9)\nelse:\n    rng = np.random.RandomState(9)\nprint('正态样本:', rng.normal(loc=100, scale=10, size=4).round(2))\nprint('泊松样本:', rng.poisson(lam=12, size=4))"),
  ],
  16: [
    example("Series()", "Series同时保存值和索引，索引可以表达业务标签。", "import pandas as pd\n\nsales = pd.Series([120, 150, 180], index=['华东', '华南', '华北'], name='sales')\nprint(sales)\nprint('华南:', sales['华南'])"),
    example("DataFrame()", "DataFrame的每个列名对应一列同长度数据。", "import pandas as pd\n\norders = pd.DataFrame({'order_id': ['A1', 'A2'], 'amount': [320, 880]})\nprint(orders)"),
    example("shape、columns 和 dtypes", "先检查表格形状、列名和数据类型，再开始分析。", "import pandas as pd\n\ndata = pd.DataFrame({'name': ['A', 'B', 'C'], 'score': [82, 91, 76]})\nprint('shape:', data.shape)\nprint('columns:', data.columns.tolist())\nprint('dtypes:\\n', data.dtypes)"),
    example("Series 对齐", "Pandas按索引标签对齐，而不是只按位置相减。", "import pandas as pd\n\neast = pd.Series({'一月': 120, '二月': 150})\nsouth = pd.Series({'二月': 98, '三月': 132})\nprint(east + south)"),
    example("head()、tail() 和 sample()", "抽查表格时同时看开头、结尾和随机样本。", "import pandas as pd\n\ndata = pd.DataFrame({'id': range(1, 6), 'value': [10, 20, 30, 40, 50]})\nprint(data.head(2))\nprint(data.tail(2))\nprint(data.sample(2, random_state=1))"),
  ],
  17: [
    example("loc", "loc使用行标签和列标签，切片通常包含结束标签。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华南', '华北'], 'amount': [320, 880, 460]}, index=['A1', 'A2', 'A3'])\nprint(orders.loc[['A1', 'A3'], ['region', 'amount']])"),
    example("iloc", "iloc使用整数位置，切片右端不包含。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华南', '华北'], 'amount': [320, 880, 460]})\nprint(orders.iloc[:2, 1])"),
    example("布尔条件筛选", "多个条件需要分别加括号，再使用&或|组合。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华南', '华北'], 'amount': [320, 880, 460]})\nselected = orders[(orders['amount'] >= 400) & (orders['region'] != '华北')]\nprint(selected)"),
    example("query()", "query适合写成接近自然语言的列条件。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华南', '华北'], 'amount': [320, 880, 460]})\nprint(orders.query(\"amount >= 400 and region != '华北'\"))"),
    example("sort_values() 与 nlargest()", "排序用于完整排名，nlargest适合快速取得Top N。", "import pandas as pd\n\norders = pd.DataFrame({'id': ['A1', 'A2', 'A3'], 'amount': [320, 880, 460]})\nprint(orders.sort_values('amount', ascending=False))\nprint(orders.nlargest(2, 'amount'))"),
    example("isin() 与 between()", "这两个方法适合表达集合筛选和区间筛选。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华南', '华北'], 'amount': [320, 880, 460]})\nprint(orders[orders['region'].isin(['华东', '华南'])])\nprint(orders[orders['amount'].between(300, 500)])"),
  ],
  18: [
    example("assign()", "assign返回包含新列的新表，适合连续处理。", "import pandas as pd\n\norders = pd.DataFrame({'amount': [320, 880], 'quantity': [2, 4]})\nresult = orders.assign(unit_price=orders['amount'] / orders['quantity'])\nprint(result)"),
    example("loc 条件更新", "loc可以只更新满足条件的行，避免链式赋值。", "import pandas as pd\n\norders = pd.DataFrame({'amount': [320, 880, 460], 'status': ['完成', '取消', '完成']})\norders.loc[orders['status'] == '取消', 'amount'] = 0\nprint(orders)"),
    example("drop()", "drop默认返回新对象，不会自动改变原表。", "import pandas as pd\n\ndata = pd.DataFrame({'id': ['A1', 'A2'], 'amount': [320, 880], 'temporary': [1, 2]})\nclean = data.drop(columns=['temporary'])\nprint(clean)"),
    example("rename()", "重命名后列名更能表达业务含义。", "import pandas as pd\n\ndata = pd.DataFrame({'amount': [320, 880], 'qty': [2, 4]})\nrenamed = data.rename(columns={'amount': 'sales_amount', 'qty': 'item_count'})\nprint(renamed)"),
    example("to_numeric()", "errors='coerce'会把无法解析的值转换为缺失值，需要后续检查。", "import pandas as pd\n\nraw = pd.Series(['320.5', 'N/A', '880'])\nparsed = pd.to_numeric(raw, errors='coerce')\nprint(parsed)\nprint('缺失数:', parsed.isna().sum())"),
    example("astype()", "分类字段可以转换为category，明确字段语义。", "import pandas as pd\n\ndata = pd.DataFrame({'region': ['华东', '华南', '华东']})\ndata['region'] = data['region'].astype('category')\nprint(data.dtypes)"),
  ],
  19: [
    example("isna()", "先统计缺失规模，再决定删除还是填充。", "import numpy as np\nimport pandas as pd\n\ndata = pd.DataFrame({'region': ['华东', None, '华北'], 'amount': [320, np.nan, 460]})\nprint(data.isna())\nprint(data.isna().sum())"),
    example("fillna()", "填充值必须符合字段含义，不能看到缺失就统一填0。", "import pandas as pd\n\ndata = pd.DataFrame({'region': ['华东', None], 'amount': [320, 880]})\ndata['region'] = data['region'].fillna('未知')\nprint(data)"),
    example("dropna()", "只有在缺失记录确实无法参与分析时才删除。", "import pandas as pd\n\ndata = pd.DataFrame({'id': ['A1', 'A2', 'A3'], 'amount': [320, None, 460]})\nclean = data.dropna(subset=['amount'])\nprint(clean)"),
    example("duplicated() 与 drop_duplicates()", "先统计重复，再按明确业务键去重。", "import pandas as pd\n\ndata = pd.DataFrame({'customer_id': ['U1', 'U2', 'U2'], 'amount': [320, 880, 880]})\nprint('重复数:', data.duplicated(subset=['customer_id']).sum())\nprint(data.drop_duplicates(subset=['customer_id']))"),
    example("quantile() 异常阈值", "IQR规则适合标记潜在异常，但不等于直接删除。", "import pandas as pd\n\nvalues = pd.Series([10, 12, 13, 15, 18, 20, 100])\nq1, q3 = values.quantile([0.25, 0.75])\nupper = q3 + 1.5 * (q3 - q1)\nprint('上界:', upper)\nprint('潜在异常:', values[values > upper].tolist())"),
  ],
  20: [
    example("Series.str.strip()", "str访问器可以批量调用字符串方法。", "import pandas as pd\n\nnames = pd.Series([' 张三 ', ' 李四'])\nprint(names.str.strip())"),
    example("Series.str.replace()", "正则替换适合清理格式不一致的文本。", "import pandas as pd\n\nphones = pd.Series(['138-0000-1234', '139 0000 5678'])\nprint(phones.str.replace(r'\\D', '', regex=True))"),
    example("to_datetime()", "无效日期可以转为NaT，但转换后必须检查缺失。", "import pandas as pd\n\ndates = pd.Series(['2026-01-05', 'invalid'])\nparsed = pd.to_datetime(dates, errors='coerce')\nprint(parsed)\nprint('无效日期数:', parsed.isna().sum())"),
    example("dt 访问器", "日期解析后通过dt访问器提取月份、星期等字段。", "import pandas as pd\n\ndates = pd.to_datetime(pd.Series(['2026-01-05', '2026-02-18']))\nprint(dates.dt.month.tolist())\nprint(dates.dt.day_name().tolist())"),
    example("cut()", "cut把连续数值转换为有序区间类别。", "import pandas as pd\n\namount = pd.Series([120, 580, 1280])\nlevel = pd.cut(amount, bins=[0, 500, 1000, float('inf')], labels=['普通', '重点', '大额'])\nprint(level)"),
  ],
  21: [
    example("read_csv()", "StringIO可以在没有真实文件的情况下练习CSV读取。", "from io import StringIO\nimport pandas as pd\n\ntext = 'id,amount\\nA1,320\\nA2,880\\n'\ndata = pd.read_csv(StringIO(text))\nprint(data)"),
    example("read_csv() 控制分隔符和类型", "读取时明确sep和dtype，可以避免编号前导零丢失；str写法兼容旧版Pandas。", "from io import StringIO\nimport pandas as pd\n\ntext = 'product_id;stock\\n001;18\\n002;24\\n'\ndata = pd.read_csv(StringIO(text), sep=';', dtype={'product_id': str})\nprint(data)\nprint(data.dtypes)"),
    example("to_numeric()", "先转换，再检查新产生的缺失值。", "import pandas as pd\n\nraw = pd.Series(['320', 'invalid', '880'])\nnumbers = pd.to_numeric(raw, errors='coerce')\nprint(numbers)"),
    example("to_csv()", "index=False可以避免把默认行号写成多余列。", "import pandas as pd\n\ndata = pd.DataFrame({'id': ['A1', 'A2'], 'amount': [320, 880]})\nprint(data.to_csv(index=False))"),
    example("to_json()", "orient='records'适合把每一行导出为一个JSON对象。", "import pandas as pd\n\ndata = pd.DataFrame({'id': ['A1', 'A2'], 'amount': [320, 880]})\nprint(data.to_json(orient='records', force_ascii=False))"),
  ],
  22: [
    example("groupby()", "分组前先明确分组维度和要计算的指标。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华东', '华南'], 'amount': [320, 880, 460]})\nprint(orders.groupby('region')['amount'].sum())"),
    example("agg()", "agg可以一次计算多个统计量；这里使用旧版Pandas也支持的写法。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华东', '华南'], 'amount': [320, 880, 460]})\nsummary = orders.groupby('region')['amount'].agg(['sum', 'mean']).rename(columns={'sum': 'total', 'mean': 'average'})\nprint(summary)"),
    example("transform()", "transform结果和原表等长，可以直接生成派生列。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华东', '华南'], 'amount': [320, 880, 460]})\norders['region_total'] = orders.groupby('region')['amount'].transform('sum')\norders['share'] = orders['amount'] / orders['region_total']\nprint(orders)"),
    example("pivot_table()", "index、columns和values分别定义透视表的行、列和指标。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华东', '华南'], 'channel': ['线上', '线下', '线上'], 'amount': [320, 880, 460]})\nprint(orders.pivot_table(index='region', columns='channel', values='amount', aggfunc='sum', fill_value=0))"),
    example("crosstab()", "crosstab适合快速统计两个分类变量的组合频数。", "import pandas as pd\n\norders = pd.DataFrame({'region': ['华东', '华东', '华南'], 'channel': ['线上', '线下', '线上']})\nprint(pd.crosstab(orders['region'], orders['channel']))"),
  ],
  23: [
    example("merge()", "连接前确认键的唯一性和预期的关系类型。", "import pandas as pd\n\norders = pd.DataFrame({'order_id': ['A1', 'A2'], 'customer_id': ['U1', 'U2']})\ncustomers = pd.DataFrame({'customer_id': ['U1', 'U2'], 'city': ['上海', '广州']})\nprint(orders.merge(customers, on='customer_id', how='left', validate='many_to_one'))"),
    example("concat()", "concat沿行或列拼接，ignore_index可以重建连续索引。", "import pandas as pd\n\nfirst = pd.DataFrame({'month': ['1月'], 'sales': [120]})\nsecond = pd.DataFrame({'month': ['2月'], 'sales': [150]})\nprint(pd.concat([first, second], ignore_index=True))"),
    example("melt()", "melt把多个指标列折叠为变量和值，适合后续分组和可视化。", "import pandas as pd\n\nwide = pd.DataFrame({'region': ['华东', '华南'], '一月': [120, 98], '二月': [150, 132]})\nprint(wide.melt(id_vars='region', var_name='month', value_name='sales'))"),
    example("pivot()", "pivot要求每个行列组合只有一个值，否则需要使用pivot_table。", "import pandas as pd\n\nlong = pd.DataFrame({'region': ['华东', '华东', '华南', '华南'], 'month': ['一月', '二月', '一月', '二月'], 'sales': [120, 150, 98, 132]})\nprint(long.pivot(index='region', columns='month', values='sales'))"),
    example("stack() 与 unstack()", "stack把列层级压到索引，unstack把索引层级展开为列。", "import pandas as pd\n\ntable = pd.DataFrame({'一月': [120, 98], '二月': [150, 132]}, index=['华东', '华南'])\nstacked = table.stack()\nprint(stacked)\nprint(stacked.unstack())"),
  ],
  24: [
    example("rolling()", "rolling用于计算连续窗口统计，窗口大小要符合业务周期。", "import pandas as pd\n\nsales = pd.Series([120, 135, 128, 160])\nprint(sales.rolling(3, min_periods=1).mean())"),
    example("cumsum()", "累计指标保留每个时间点的进展，不只是最终总和。", "import pandas as pd\n\nsales = pd.Series([120, 150, 180])\nprint(sales.cumsum())"),
    example("pct_change()", "增长率要说明比较周期，并检查第一行缺失值。", "import pandas as pd\n\nsales = pd.Series([100, 120, 108])\nprint(sales.pct_change().round(3))"),
    example("describe()", "describe快速查看计数、均值、分位数和最大最小值。", "import pandas as pd\n\nvalues = pd.Series([10, 12, 13, 15, 20])\nprint(values.describe())"),
    example("value_counts()", "频数统计适合检查分类分布，也可以计算占比。", "import pandas as pd\n\nregions = pd.Series(['华东', '华南', '华东', '华北'])\nprint(regions.value_counts())\nprint(regions.value_counts(normalize=True).round(2))"),
    example("corr() 与 rank()", "相关系数描述共同变化，rank用于生成排序名次，二者都不能直接证明因果。", "import pandas as pd\n\ndata = pd.DataFrame({'amount': [320, 880, 460], 'items': [2, 4, 1]})\nprint(data.corr())\ndata['rank'] = data['amount'].rank(ascending=False)\nprint(data)"),
  ],
};
